package org.test;

public class BaseClass {
	public void BrowserconfigChrome() {
		Webdriver Manager.chromedriver().setup();
		Webdriver driver = new chromeDriver();
	}
	
	public void BrowserConfigFirefox() {
		WebDriver Manager.firefoxdriver().setup();
		WebDriver driver=new firefox driver();
	}
	
public WebElement locateByClassname(String name) {
WebElement findElement= driver.findElement(By.classname(name));
return findElement;
	
public WebElement locateByxpath(String text) {
WebElement findElement= driver.findElement(By.xpath(text));
	
public WebElement locateByTagname(String name) {
WebElement findElement= driver.findElement(By.xpath(tagname));

public WebElement locateByxpath(String id) {
WebElement findElement= driver.findElement(By.xpath(radiobutton));






}
		
	}
}
}
	}
